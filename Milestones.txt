1. Create appropriate classes, complete with data fields and methods, to handle application data on geographic regions as described below under "Model".

2. Add a class, complete with data fields and methods, to the model to allow the model to correctly interact with the controller and the views.

3. Create appropriate classes, complete with data fields and methods, for the views described below.

4. Create an appropriate class, complete with data fields and methods, for the controller as described below.

> Develop and use a proper design

> Use proper documentation and formatting